# 测试结果 · 2026-09-05

上海如静知华信息科技有限公司 · https://www.zhuatech.cn/

## 最终结果

| 检查 | 结果 |
|---|---|
| Java 后端 HTTP / 业务与一致性测试 | 41 项通过，失败 0，错误 0，跳过 0 |
| 其中：关联业务验收步骤 | 29 项，见 acceptance.json |
| 其中：权限、并发与其他回归 | 12 项 |
| 数据库 | 真实 MySQL 8.4，独立空测试库，V1/V2 迁移均执行 |
| 前端单元测试 | 6 项通过 |
| 前端生产构建 | Vite build 成功 |
| H5 实际操作 | 登录、新建、刷新保留、详情、管理端、审计页通过 |
| 手机适配 | 390 × 844 视口，页面无横向溢出 |
| 浏览器最终验收 | 无 pageerror / console error |
| Docker Compose 配置校验 | 通过 |
| 公开依赖风险检查 | 10 个前端锁文件依赖树相同，npm audit 本次报告 0 个已知漏洞 |

后端使用 Java 21 + Spring Boot 4.0.7；前端使用 Vue 3.5.40 / Vite 8.1.5。Maven 报告位于 backend/target/surefire-reports（构建产物不应上传仓库）。本文件摘录真实执行结果，不存令牌、私钥或真实客户数据。

## 验收内容

审批先预留可借库存，发货再扣减在库并增加在外数量。每次归还分别更新完好库存与损坏库存，阻止超量归还；支持延期审批、未还数量检查和损坏样品修复或报损。

所有接口测试通过实际 HTTP 调用进入 Spring 服务，关联数据通过再次读取或查询数量与金额进行验证。拒绝操作会核对没有新增成功业务流水与成功审计；并发测试覆盖同键重试和不同请求竞争同一版本。最新记录使用数据库顺序号，专门测试同一时间戳的排序。

界面截图：[业务工作台](images/workbench.png)、[记录与操作轨迹](images/record-detail.png)、[管理端](images/admin.png)、[移动端](images/mobile.png)。截图内容为测试业务数据，不是静态设计稿。

## 范围限制

本次不包含第三方真实支付、邮件或硬件接入，不含生产容量压测、故障切换、渗透测试及行业合规认证。npm audit 仅反映检查时公开的前端依赖漏洞信息，不是全栈安全审计。

一期面向业务展示样机/实物样品，不属于临床或生物样本 LIMS。无冷链监控、仪器结果采集、寄件支付或运单平台实联；物流单号由使用方填写。

## 复现

```bash
sh scripts/test.sh
```

默认使用 H2；在专用新建 MySQL 测试库设置 TEST_DB_URL / TEST_DB_USER / TEST_DB_PASSWORD 后执行 `mvn test` 可复现 MySQL 验收。禁止将测试运行指向生产库。
